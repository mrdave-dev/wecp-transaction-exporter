{
  "name": "wecp-expenses",
  "version": "0.1.0",
  "description": "Google Apps Script tooling for WECP expense allocation workflow.",
  "private": true,
  "scripts": {
    "build": "tsc --project tsconfig.json && cp appsscript.json dist/",
    "clean": "rm -rf dist",
    "deploy": "npm run build && npx clasp push",
    "clasp": "npx clasp"
  },
  "devDependencies": {
    "@google/clasp": "^2.5.0",
    "@types/google-apps-script": "^2.0.7",
    "typescript": "^5.4.0"
  }
}
